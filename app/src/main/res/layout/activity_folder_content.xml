<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Folder Title (optional) -->
    <TextView
        android:id="@+id/folderTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Family Folder"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="8dp" />

    <!-- RecyclerView to Display Photos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewFolder"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Add Photo Button -->
    <Button
        android:id="@+id/addButtonInFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Photo"
        android:layout_gravity="center_horizontal"
        android:padding="8dp" />

    <!-- Label EditText -->
    <EditText
        android:id="@+id/editTextLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:padding="12dp"
        android:hint="Enter Label (e.g., Mom, Dad)"
        android:textSize="16sp"
        android:visibility="gone" /> <!-- Initially GONE, shown later -->

    <!-- Save Button to save photo -->
    <Button
        android:id="@+id/saveButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Save"
        android:layout_gravity="center_horizontal"
        android:visibility="gone" /> <!-- Initially GONE, shown later -->
</LinearLayout>

